// Test file for lint-staged validation with TypeScript
export interface TestConfig {
  name: string;
  version: string;
}

export const config: TestConfig = {
  name: 'lint-staged test',
  version: '1.0.0',
};

export default config;
